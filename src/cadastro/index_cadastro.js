import React from 'react';
import ReactDOM from 'react-dom/client';
import './index_cadastro';
import App from './app_cadastro';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// Não há necessidade de importar ou usar reportWebVitals
